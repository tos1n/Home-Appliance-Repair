<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ffive.seg2105ffiveproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".SignUpActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".AddServiceActivity" />
        <activity android:name=".WelcomeAdminActivity" />
        <activity android:name=".EditDeleteServiceActivity" />
        <activity android:name=".ServiceList" />
        <activity android:name=".UserList" />
        <activity android:name=".ServiceProfile" />
        <activity android:name=".WelcomeServiceProvider" />
        <activity android:name=".ProviderAvailabilities" />
        <activity android:name=".ServicesListProvider" />
        <activity android:name=".AddOfferedService" />
        <activity android:name=".WelcomeHomeOwner"></activity>
    </application>

</manifest>